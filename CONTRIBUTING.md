# 参与维护

这不是“写完就不动”的教程。产品入口、标的数量、费率、杠杆、交易时段、发行人和地区准入都可能变化。

## 报告事实变化

优先使用 GitHub 的“来源或事实纠错”Issue 模板，并提供：

1. 文件与标题，或来源 ID；
2. 仓库当前写法；
3. 建议改法；
4. 官方/一手来源 URL；
5. 你实际核验页面的日期；
6. 如有截图，先删除姓名、UID、余额、订单和证件信息。

## 修改规则

- 关键产品事实至少需要一个一手来源；
- 数量、费率、杠杆、版本、活动和开放地区必须写明核验日期；
- 宣传页与产品条款冲突时，明确列出冲突，不隐藏；
- 不把“1:1 backed”改写成“用户直接持有股票”；
- 不把“官网支持申请”改写成“所有中国大陆用户都能开户”；
- 费用优惠链接必须保留透明 CPS 披露，不能伪装成普通官方链接；
- 不提交用户密码、验证码、私钥、助记词、证件或未脱敏账户截图。

## 更新来源库

事实修改完成后，同时更新：

1. 对应 Markdown 正文；
2. `SOURCES.md` 的来源记录；
3. `data/sources.json` 的 `supports`、`published_or_updated`、`last_verified` 和 `review_after_days`；
4. `CHANGELOG.md`。

本地运行：

```bash
python3 scripts/verify_sources.py --no-network
python3 scripts/verify_sources.py
```

第一条检查 JSON、复核期限、内部链接、来源 ID 和邀请码；第二条还会测试一手 URL 可达性。网络测试通过不代表语义正确，仍需人工重新阅读。

## 新增平台或产品

至少回答：服务实体、产品类别、用户实际持有物、托管/发行人、股息与投票、交易时段、全部费用、杠杆与强平、转仓/提链/赎回、官方来源与核验日期。
